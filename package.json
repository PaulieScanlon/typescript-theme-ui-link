{
  "private": true,
  "name": "typescript-theme-ui-link",
  "version": "1.0.0",
  "workspaces": [
    "demo"
  ],
  "scripts": {
    "develop": "yarn workspace demo gatsby develop",
    "build": "yarn workspace demo gatsby build",
    "clean": "yarn workspace demo gatsby clean",
    "serve": "yarn workspace demo gatsby serve",
    "storybook": "NODE_ENV=production yarn workspace demo start-storybook -s ./public -p 6006 --ci",
    "build-storybook": " NODE_ENV=production yarn workspace demo build-storybook"
  }
}
